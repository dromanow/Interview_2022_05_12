# Generated by Django 4.0.4 on 2022-05-23 17:53

from django.db import migrations


def forward(app, se):
    Author = app.get_model('library', 'Author')
    alias = se.connection.alias
    Author.objects.using(alias).bulk_create([
        Author(first_name='Александр', last_name='Пушкин', birthday_year=1799),
        Author(first_name='Александр', last_name='Грин', birthday_year=1880)
    ])


def reverse(app, se):
    Author = app.get_model('library', 'Author')
    alias = se.connection.alias
    Author.objects.using(alias).all().delete()


class Migration(migrations.Migration):

    dependencies = [
        ('library', '0001_initial'),
    ]

    operations = [
        migrations.RunPython(code=forward, reverse_code=reverse, hints={'target_db': 'other'})
    ]
